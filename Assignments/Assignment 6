const popluation = 55;
const country = 'Portugal';
if(popluation > 33){
    console.log(`${country}'s population is above average`);
} else {
    console.log(`${country}'s population is ${33 - popluation} million below average`);
}

